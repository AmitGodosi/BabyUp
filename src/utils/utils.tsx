import React from "react";
import { Grid } from "@material-ui/core"

export const renderFooterHandler = (className?: string) => {
	return (
		<Grid className={className}>
			Footer
		</Grid>
	);
}
